//Utilização do FOR para "arrumar" os elementos
let val = [0,1,7,4,2,9]
console.log(`O vetor tem ${val.length} elementos`)
for(let pos = 0; pos < val.length; pos++){
    console.log(`A posição ${pos} tem o valor ${val[pos]}`)
}